import { createContext } from 'react';

export const ClaimProfileFormContext = createContext<any>(null);
export const DepositSingleFormContext = createContext<any>(null);
export const DepositAnotherFormContext = createContext<any>(null);
export const SearchWalletFormContext = createContext<any>(null);
